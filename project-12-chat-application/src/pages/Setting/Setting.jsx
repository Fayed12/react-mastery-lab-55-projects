import styles from './Setting.module.css';

const Setting = () => {
    return (
        <div className={styles.container}>
            <h1>Settings</h1>
        </div>
    );
};

export default Setting;
