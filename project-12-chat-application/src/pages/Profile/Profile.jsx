import styles from './Profile.module.css';

const Profile = () => {
    return (
        <div className={styles.container}>
            <h1>Profile</h1>
        </div>
    );
};

export default Profile;
