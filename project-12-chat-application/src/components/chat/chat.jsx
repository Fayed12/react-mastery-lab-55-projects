// local
import styles from './chat.module.css';

// one you check the chat id the user clicked === cureentchat id its done successfully in home.jsx , then
// check if there is chat while it true, dispaly chat screen and display chat messages, if not display empty screen
// use redux to get the current chat id and chat messages
// make the update chat messages with snapshot
// 

const Chat = () => {
    return (
        <div className={styles.container}>
            <h1>chat</h1>
        </div>
    );
};

export default Chat;
