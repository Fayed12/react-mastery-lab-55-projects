import styles from './MessageList.module.css';

const MessageList = () => {
    return (
        <div className={styles.container}>
            MessageList
        </div>
    );
};

export default MessageList;
