import style from './cartItem.module.css';

const CartItem = () => {
    return (
        <div>
            <h1 className={style.name}>CartItem</h1>
        </div>
    );
};

export default CartItem;
