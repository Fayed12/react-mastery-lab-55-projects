import style from './searchBar.module.css';

const SearchBar = () => {
    return (
        <div>
            <h1 className={style.name}>SearchBar</h1>
        </div>
    );
};

export default SearchBar;
