const API_BASE_URL = "http://localhost:5000";

export const API_ROUTES = {
    site: `${API_BASE_URL}/site`,
    users: `${API_BASE_URL}/users`,
    posts: `${API_BASE_URL}/posts`,
    comments: `${API_BASE_URL}/posts/comments`,
};

export default API_BASE_URL;
